merge_protections_settings:
  reporting_method: check-runs

merge_protections:
  - name: BB exact-head staging deployment
    if:
      - base = main
    success_conditions:
      - deployment-success = staging
